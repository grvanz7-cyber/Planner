(() => {
  const PAGE = document.getElementById("page");
  const SUBJECTS_NAV = document.querySelector('.nav-item[data-page="subjects"]');
  function esc(v){return String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));}
  function data(){return window.PlannerData.getData();}
  function route(){const p=location.hash.replace(/^#/,"").split("/");if(p[0]==="subjects")return{type:"subjects"};if(p[0]==="subject"&&p[1])return{type:"subject",id:p[1]};return null;}
  function active(){if(SUBJECTS_NAV)SUBJECTS_NAV.classList.add("active");}
  function renderSubjects(){active();const subjects=data().subjects;PAGE.innerHTML=`<div class="subjects-feature"><div class="subjects-feature-header"><div><h2>Subjects</h2><p>Your courses, units, grades, and academic progress.</p></div><button class="subjects-feature-button" id="subjectsAddButton">+ Add Subject</button></div>${subjects.length?`<div class="subjects-feature-grid">${subjects.map(card).join("")}</div>`:`<div class="subjects-feature-empty"><strong>No subjects yet</strong>Add your first subject to get started.</div>`}</div>`;document.getElementById("subjectsAddButton").onclick=showAdd;PAGE.querySelectorAll("[data-subject-id]").forEach(c=>c.onclick=()=>location.hash=`subject/${c.dataset.subjectId}`);}
  function card(s){return `<article class="subjects-feature-card" data-subject-id="${esc(s.id)}"><div class="subjects-feature-icon" style="background:${esc(s.color||"#e5ede7")}">${esc(s.icon||"📚")}</div><h3>${esc(s.name)}</h3><div class="subjects-feature-meta">Current grade: —<br>Current unit: —</div></article>`;}
  function showAdd(){const b=document.createElement("div");b.className="subjects-feature-modal-backdrop";b.innerHTML=`<div class="subjects-feature-modal" role="dialog" aria-modal="true"><h2>Add Subject</h2><form class="subjects-feature-form" id="subjectsFeatureForm"><div class="subjects-feature-field"><label for="subjectName">Name</label><input id="subjectName" required autofocus></div><div class="subjects-feature-field"><label for="subjectIcon">Icon</label><input id="subjectIcon" value="📚" maxlength="4"></div><div class="subjects-feature-field"><label for="subjectColor">Colour</label><input id="subjectColor" type="color" value="#e5ede7"></div><div class="subjects-feature-field"><label for="subjectPeriod">Academic period</label><select id="subjectPeriod"><option>Semester 1</option><option>Semester 2</option></select></div><div class="subjects-feature-actions"><button type="button" class="subjects-feature-secondary" id="subjectCancel">Cancel</button><button class="subjects-feature-button" type="submit">Add Subject</button></div></form></div>`;document.body.appendChild(b);b.querySelector("#subjectCancel").onclick=()=>b.remove();b.onclick=e=>{if(e.target===b)b.remove();};b.querySelector("form").onsubmit=e=>{e.preventDefault();const s=window.PlannerData.create("subjects",{name:b.querySelector("#subjectName").value.trim(),icon:b.querySelector("#subjectIcon").value.trim()||"📚",color:b.querySelector("#subjectColor").value,academicPeriod:b.querySelector("#subjectPeriod").value});b.remove();renderSubject(s.id);};}
  function renderSubject(id){active();const s=window.PlannerData.find("subjects",id);if(!s){location.hash="subjects";return;}PAGE.innerHTML=`<div class="subjects-feature"><div class="subjects-feature-header"><div><button class="subjects-feature-secondary" id="subjectBack">← Back</button><h2 style="margin-top:16px">${esc(s.icon||"📚")} ${esc(s.name)}</h2><p>Current grade: — &nbsp; · &nbsp; Current unit: —</p></div></div><section><h3>Units</h3><div class="subjects-feature-empty"><strong>No units yet</strong>Units will appear here as they are added.</div></section></div>`;document.getElementById("subjectBack").onclick=()=>location.hash="subjects";}
  function render(){const r=route();if(!r)return;if(r.type==="subjects")renderSubjects();else renderSubject(r.id);}
  window.addEventListener("hashchange",render);window.addEventListener("planner:data-changed",()=>{if(route())render();});if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",render);else render();
})();
